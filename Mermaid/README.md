# Config in same one file

See: "Sample\_config\_in\_mdd.mdd"

# Config with Json

See: "mermaid.sequenceConfig.json" in this folder
Compile with ```mmdc -i fontTest.mmd -c mermaid.sequenceConfig.json -o 
output.png```
